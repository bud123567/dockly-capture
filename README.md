# dockly-capture

Source for the Dockly Capture app.

## Status

Repository scaffolded. Application files are pending export from the
ChatGPT-hosted preview at `macharbor-caleb.amused-pike-8012.chatgpt.site`,
which requires an authenticated session to access.

## Layout

Once the export lands, the app files (`index.html`, styles, scripts, and
assets) live at the repository root so the site can be served statically —
GitHub Pages, Netlify, or any static host.

## Local preview

```sh
python3 -m http.server 8000
```

Then open http://localhost:8000
